# Copy to .env. Never commit secrets.
# Discovery live runs only. `make test` does not need this.
ANTHROPIC_API_KEY=

# Target app
LEGACYBANK_HOST=127.0.0.1
LEGACYBANK_PORT=8081
LEGACYBANK_PORT_B=8082
LEGACYBANK_IDLE_SECONDS=60
LEGACYBANK_LATENCY=1

# Operator console (mock UI, real session seam)
OPERATOR_PORT=8090
