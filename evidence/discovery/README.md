Discovery evidence. Live LLM runs land here. Cassette runs write under pytest tmp unless copied.

Mandatory live run: `dsc_live/` (Claude Sonnet 4.6 against tenant A). Stop reason `GOAL_REACHED`. Auto-replay extracted `$4,215.60` / `active`.
